# RailYatra — Payment Page

Minimal front-end project for train booking payment flow.

## Run locally
Open `payment.html` in a browser or run a static server:
- npx http-server . -p 8080
- npx live-server

## Structure
- payment.html
- css/
- js/
- assets/

## Git
Initialize and push:
- git init
- git add .
- git commit -m "Initial commit"
- git branch -M main
- git remote add origin <repo-url>
- git push -u origin main

License: MIT (see LICENSE)